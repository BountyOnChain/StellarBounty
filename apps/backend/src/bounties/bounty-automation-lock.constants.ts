/** PostgreSQL advisory lock id for deadline automation (S-B #79). */
export const BOUNTY_AUTOMATION_LOCK_ID = 79;

/** PostgreSQL advisory lock id for settlement scheduler (S-B #550). */
export const SETTLEMENT_SCHEDULER_LOCK_ID = 80;
