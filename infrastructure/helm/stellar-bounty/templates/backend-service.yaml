{{- if and .Values.backend.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "stellar-bounty.fullname" . }}-backend
  labels:
    {{- include "stellar-bounty.componentLabels" (dict "ctx" . "component" "backend") | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "stellar-bounty.componentSelectorLabels" (dict "ctx" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
